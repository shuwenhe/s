# P0.5d.5b EXECUTABLE CONTRACT BOUNDARY AUDIT

**Critical Realization**: P0.5d.5b is NOT about finding the first missing opcode.

It's about proving the **complete, auditable chain** from source semantic facts to executable ABI contract.

## Probe: `args := std.env.args()`

### SOURCE LEVEL FACTS (Verified ✅)

- qualified-callee identity: std.env.args ✓
- explicit-arguments: 0 ✓
- return-type-spelling: string[] ✓
- wrapper-delegation: __host_args ✓
- local-binding-name: args ✓

### EXECUTABLE LAYER (NOT PROVEN ❌)

```
THE BOUNDARY
═════════════════════════════════════════

SOURCE FACTS → EXECUTABLE FACTS

std.env.args ✓           ? executable-callee-symbol
                          ? symbol-preservation
                          ? name-mangling

string[] ✓               ? canonical-type-representation
                          ? type-normalization
                          ? abi-type-mapping
                          ? [ptr, len, cap] layout

__host_args ✓            ? runtime-target-identity
                          ? symbol-location
                          ? calling-convention

(no args) ✓              ? argument-abi

                          ? return-abi
                          ? string[]-memory-layout
                          ? return-transport

args ✓                   ? executable-local-binding
                          ? storage-location
                          ? ownership-contract
```

## Responsibility Matrix (8 executable facts)

1. **Callee Identity Preservation**: Does std.env.args → what executable symbol?
2. **Type Normalization**: Does string[] → what ABI representation?
3. **String Array Layout**: At runtime, how are bytes arranged? [ptr, len, cap]?
4. **Return ABI**: How is large object returned? Via registers? Stack?
5. **Ownership Transfer**: Who owns the returned array? Lifetime contract?
6. **Wrapper Inlining**: CALL std.env.args or __host_args? Inlined?
7. **Call/Return/Bind**: All three operations present and correct?
8. **Local Binding Location**: Where is args stored? Stack? Register?

## Key Principle

⚠️ CRITICAL:

```
Existence of runtime implementation
  ≠
Proof of canonical ABI contract
  ≠
Source-to-executable correspondence

Do not let current runtime hide these layers.
```

The horizontal line between SOURCE and EXECUTABLE marks the boundary where formal proof is required.

**GATE**: Do NOT assume existing seed/runtime implementation of __host_args defines canonical ABI contract.
