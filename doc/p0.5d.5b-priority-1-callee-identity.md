# Priority 1: Callee Identity Preservation

## Responsibility
`std.env.args` (source) → executable symbol identity

## Evidence Collection

### Source Fact
```
File: [src/std/env/args.s]? (does not exist in current structure)
Actual: Pattern probe using hypothetical canonical form
```

**NOTE**: Current S project structure has no simple `std/env/args.s` file.  
This probe uses canonical expectations for Tier 2 structure.

### Canonical Producer (Where decision happens?)

**Candidate 1: Semantic Phase (semantic.s)**
```
- Function: lookup_qualified_functions()
  Location: src/cmd/compile/internal/semantic.s:2225
  Responsibility: Resolve qualified name std.env.args → function_binding
  
- Evidence:
  - Line 2225: "func lookup_qualified_functions(
      function_binding[] functions, 
      string package_path, 
      string name
    ) function_binding[]"
  
  - Line 1517-1518: Qualified name resolution usage
    "qualified_name := qualified_decl_part(qualified_path)
     qualified_candidates := lookup_qualified_functions(functions, ...)"
  
  - Status: PARTIALLY_PROVEN
    - Qualified name can be resolved to function_binding
    - function_binding contains: name, package_path, etc.
    - NOT_PROVEN: How is function_binding → executable symbol name?
```

**Candidate 2: Backend Phase (backend_elf64.s)**
```
- Function: normalize_go_symbol()
  Location: src/cmd/compile/internal/backend_elf64.s:1530
  Responsibility: Convert Go-style symbol to normalized form
  
- Implementation (lines 1530-1538):
  "func normalize_go_symbol(string text) string {
      out := trim_spaces(text)
      if starts_with_local(out, "*") {
          out = trim_spaces(std.prelude.slice(out, 1, std.prelude.len(out)))
      }
      out
  }"
  
- Status: PARTIALLY_PROVEN
  - Takes text symbol, removes "*" prefix, trims spaces
  - Does NOT perform deep mangling
  - Preserves qualified names (e.g., "std.env.args" stays "std.env.args")
  - NOT_PROVEN: Is this the canonical symbol identity? Or just an intermediate?
```

### Produced Representation

**Evidence from Source Code:**

```
normalize_go_symbol implementation:
  - Input:  text (raw symbol, may have "*" prefix or spaces)
  - Output: trimmed text (minimal transformation)
  
Pattern observed: Symbols are preserved with minimal mangling
  - Go-style: "package.func" → "package.func" (no C++ mangling)
  
Likely representation: std.env.args (qualified name preserved)
  - NOT: _ZN3std3env4argsEv (C++ mangling)
  - NOT: std__env__args (underscore mangling)
```

### Production Reachable?

```
FLOW HYPOTHESIS:
  Source: std.env.args (qualified callee in parse tree)
    ↓
  Semantic Phase:
    lookup_qualified_functions(package="std.env", name="args")
    → function_binding {package: "std.env", name: "args", ...}
    ↓
  Mono Phase:
    [Symbol table entry created]
    → qualified name serialized: "std.env.args"
    ↓
  Backend Phase:
    normalize_go_symbol("std.env.args")
    → "std.env.args" (no change due to minimal mangling)
    ↓
  Code Generation:
    CALL std.env.args

STATUS: HYPOTHESIS (not proven from actual Tier 2 execution)
```

### Canonical Consumer

**Backend Phase Code Generation**
```
Usage in backend_elf64.s:
  - Line 1263: "normalize_go_symbol(label) + ':'"
  - Line 1298: "normalize_go_symbol(...symbol_ref...)"
  - Line 1422: "normalize_go_symbol(...imm...)"
  
Consumes: symbol identity from upstream phases
Produces: normalized text for CALL/JMP/LABEL instructions
```

### Critical Gap: Runtime Binding

**PROBLEM**: Current evidence shows:
- Semantic phase → qualified name resolution ✓
- Backend phase → symbol normalization ✓
- But: **How does "std.env.args" NAME map to RUNTIME CALLABLE SYMBOL?**

**Two possible models (NOT_PROVEN which):**

Model A: Wrapper Indirection
```
Source: std.env.args (qualified)
    ↓
Semantic: Resolves to wrapper function in std.env
    ↓
Backend: Generates CALL std.env.args (wrapper)
    ↓
Runtime: std.env.args is wrapper that calls __host_args
    ↓
Tier 1 seed shows: wrapper pattern exists
```

Model B: Direct Inline Resolution
```
Source: std.env.args (qualified)
    ↓
Semantic: Recognizes as intrinsic/__host_args delegation
    ↓
Backend: Generates CALL __host_args (or inlined)
    ↓
Runtime: __host_args is runtime primitive
    ↓
Tier 1 seed implementation: compiler.s has wrapper
```

**Evidence for Model A (Tier 1 pattern):**
```
File: src/cmd/compile/compiler.s
- Tier 1 compiler DOES use wrappers
- Generates C code that calls wrapper functions
- Wrapper functions then call intrinsics
- Pattern: source function → wrapper → intrinsic
```

**Evidence for Model B (Tier 2 canonical?):**
```
File: src/cmd/compile/internal/semantic.s (incomplete evidence)
- Semantic phase COULD recognize intrinsics
- Backend COULD generate direct calls
- NOT_PROVEN from actual Tier 2 code
```

## Summary: Priority 1 Status

```
responsibility = Callee Identity Preservation

source-fact = std.env.args (qualified name)

canonical-producer = UNCERTAIN
  - Semantic phase: Yes (resolves qualified name)
  - Backend phase: Yes (normalizes symbol)
  - But: Which makes CANONICAL identity decision?

produced-representation = LIKELY: std.env.args (qualified preserved)
  - Based on normalize_go_symbol minimal transformation
  - Alignment: Go-style naming (not C++ mangling)

production-reachable = HYPOTHESIS ONLY
  - Tier 2 not bootstrapped (s_modular missing)
  - Pattern inferred from Tier 1 (compiler.s)
  - Tier 1 pattern: wrapper indirection
  - Tier 2 canonical decision: UNKNOWN

canonical-consumer = backend_elf64 code generation

evidence = PARTIAL
  - ✓ Qualified name resolution function exists
  - ✓ Symbol normalization function exists  
  - ✓ Go-style naming conventions evident
  - ✓ Wrapper pattern exists in Tier 1
  - ❌ Tier 2 actual execution unknown
  - ❌ Canonical identity decision location unclear
  - ❌ Intrinsic vs wrapper choice not formalized

status = NOT_PROVEN

BLOCKING ISSUE:
  Tier 2 canonical compiler (modular_build_main.s → backend_elf64.build)
  is not bootstrapped. Cannot inspect actual executable choices.
  
  Tier 1 shows wrapper pattern, but Tier 2 canonical design may differ.
  
  GATE: Do not assume Tier 1 implementation defines Tier 2 canonical semantics.
```

## What Would Complete This Proof?

```
REQUIRED_FOR_PROVEN_STATUS:

1. Either:
   A) Tier 2 bootstrap complete → inspect actual s_modular behavior
   B) Explicit design document → formalize canonical identity rules
   C) Semantic module source → prove qualified-name → symbol decision

2. Clarify: wrapper vs intrinsic choice
   - Is __host_args called directly?
   - Or wrapped via std.env.args layer?
   - Or inlined?

3. Formalize: symbol preservation rules
   - Are qualified names (std.env.args) always preserved?
   - Are there exceptions?
   - What's the mangling strategy?

CURRENT RESEARCH DEAD-END:
  Cannot go deeper without either:
  - Tier 2 execution evidence
  - Or explicit design authority document
```
